# Step 2 — Build Docker Image

Purpose:

Commit the running FlagOS container into a docker image.

Command example:

docker commit qwen3.5_flagos flagos:2603031041

Execute tool:

tools/build_image.sh

Output:

Local docker image